import 'package:flutter/material.dart';

class ImmunizationData extends StatelessWidget {
  const ImmunizationData({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
